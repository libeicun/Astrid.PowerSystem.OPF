 #include <stdio.h>
#include "CList.h"


/*template <class T>
ostream& operator <<(ostream& outer, CList<T> mylist )
	{
		CListNode<T> *p=mylist.getHead();
		outer<<"\nNow output the datas...\n\n";
		for (;p!=NULL;p=p->getNext() )
		{
			outer <<p->getData()<<" ";

		}
		outer<<"\nThat's all.\n\n";
		return outer;
	}


*/